1. need to disable the traveller printing view. -> or change it into a label designer/ printing app page?
2. study if it is feasible to log product into box "on the fly" to ensure box data is always updated
in real time, avoiding edge case where box is packed in half, but OP turns off the web, leading to 
next time open, have to unpack that semi finfish box to scan again. 
3.Fix the 'qty' error on the WIP warehouse system, need to log one more column which is "responsible depth"
and how is the UX/UI for switching responsible depth?
REMOVE UNIQUE CONSTRAINT ON THE BATCH_ID of wip_inventory table, in which, so that each department can have their own
table view of themselves when they hold the same batch (ie transferring from 1 dept to another)
4. Add the feature of "Auto cleaning after a specific interval" to avoid trash in dashboard
5. Add error recovery page (example: undo outsource allocation mistake)